server:
  port: 8080
  host: "0.0.0.0"

database:
  host: "localhost"
  port: 5432
  user: "postgres"
  password: "postgres"
  name: "pharmacy_shop"
  ssl_mode: "disable"

logging:
  level: "info"
  format: "text" 